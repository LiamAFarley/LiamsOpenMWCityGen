profile across the border before erosion.

The failed profile experiments already demonstrated why that approach is bad: raw mirroring produced spikes, Euclidean profile assignment crossed corners and striped the surface, and edge-oriented inward continuation generated hundreds of thousands of conflicting profile claims.

2. Remove global nearest-vertex fill

In:

src/procgen/terrain_blend.py

remove:

tam_h = fill_missing_from_edges(
    arrays["tam_h"]
)

from production context construction.

Do not replace it with another global nearest-neighbor method.

Keep the raw field:

tam_h_raw = arrays["tam_h"]

and generate only terrain that actually needs to exist.

The current global fill is both semantically crude and the source of the flat missing-cell regions.

3. Fix corpus height ownership before synthesizing holes

This is worth fixing now because the current corpus distinguishes cell ownership from neither actual VHGT availability nor fallback source.

cell_owner is resolved later-owner-wins, but oth_h is populated by iterating all owner sources and simply skipping records with no VHGT. A later winning owner with a heightless LAND record therefore does not explicitly clear an earlier owner's height contribution from the shared oth_h raster.

Add explicit cell-level height provenance to the corpus.

During source scanning/building, collect:

source_vhgt_cells[source_index]

containing cells whose LAND actually has heights_thu.

Then emit a cell-grid:

cell_height_source

with these semantics:

0
    no authoritative height exists;
    terrain must be synthesized

base_code
    use Tamriel/base VHGT

owner_code
    use the winning owner's VHGT

Resolution rule for each non-void cell:

winner = cell_owner[cell]

if winner_has_vhgt:
    cell_height_source[cell] = winner

elif base_has_vhgt_at_same_cell:
    # user-approved owner-stub fallback
    cell_height_source[cell] = base_code

else:
    # LAND position exists but nobody provides usable height
    cell_height_source[cell] = 0

Do not fall back to a losing owner merely because oth_h happens to contain old finite pixels.

Also export:

synth_height_cells = (
    (cell_owner != 0)
    & (cell_height_source == 0)
)

This becomes the authoritative missing-terrain mask.

4. Rebuild oth_h using winning height ownership

When filling the owner raster, only write an owner's heights for cells where that source is the winning height source.

Conceptually:

if cell_height_source[cy, cx] != source_code:
    continue

This prevents stale heights from an earlier overlapping owner from remaining under a later heightless winner.

For cells using base fallback:

do not write oth_h;
own_view later resolves from base terrain

For synthesis-required cells:

oth_h and base may both remain NaN initially;
the synthesis stage will create the working terrain.
5. Generate missing cells with the same AMG machinery, not nearest fill

Create something like:

src/procgen/terrain_inpaint.py

or keep it in terrain_blend.py initially if minimizing refactoring is preferable.

Function:

synthesize_missing_heights(
    base_field,
    owner_field,
    cell_owner,
    cell_height_source,
    required_region,
    ...
)

The unknown region is:

missing_cell_mask = (
    cell_owner != 0
) & (
    cell_height_source == 0
)

Rasterize only the missing cells needed by the current solve region plus a small context halo.

Do not fill global world void.

6. Use direct harmonic interpolation for the missing-cell macro surface

For each connected required missing component:

unknown:
    missing vertices inside required cells

fixed boundary:
    neighboring valid Tamriel heights
    + neighboring valid winning-owner heights

Solve absolute height:

L
uu
	​

H
u
	​

=b

using exactly the same direct Laplace + AMG approach that is now working well for the seam solver.

Do not solve LᵀL.

Do not add nearest-neighbor copying.

Do not clamp.

Because AMG is already solving ~2 million unknowns in five iterations, cell-hole inpainting should be cheap relative to everything else. The latest crop measured ~1.35 s setup and ~0.88 s solve for a 2.08M-unknown system.

For a cell surrounded by real terrain this produces:

north edge ───────── known
       \             /
        \ smooth 2-D/
west    |   fill    | east
edge    |           | edge
        /           \
south edge ───────── known

rather than:

nearest edge value
nearest edge value
nearest edge value
nearest edge value
7. Fail explicitly on truly unconstrained missing components

If a missing component has no finite neighboring boundary heights, do not quietly nearest-fill it.

Return a diagnostic such as:

missing component N has no authoritative boundary

Those cases can then receive an explicit ocean/bathymetry policy if they actually exist.

Do not let a generic nearest-neighbor routine silently invent them.

8. Use the synthesized field as the relief input

After missing-cell synthesis:

working_base = synthesized_tamriel

Then run:

relief_scale(
    working_base,
    terrain_relief_config,
)

The current relief transform can remain unchanged initially.

The important change is that its input contains a continuous real surface rather than nearest-neighbor slabs.

9. Simplify the current corner behavior immediately

The latest report identifies the only huge C1 outlier:

owner step = 0 GU
generated first step = 12,104 GU

and attributes it to the special corner-diagonal claim overriding incompatible adjacent edge claims.

Remove that special diagonal hard anchor.

In current solve_surface(), delete the logic that computes:

desired = (
    h_corner
    + dh1
    + dh2
    - target[uy, ux]
)

for the diagonal corner vertex. That code is still present in the current source.

For a true corner:

keep the actual seam vertex exact
keep ordinary anchors on the adjacent straight-edge samples
do NOT create a special diagonal hard anchor

Let the harmonic solution determine the diagonal interior point.

This is preferable to creating another corner model before erosion.

10. Keep the present harmonic seam solve only as a broad pre-erosion bridge

The current harmonic correction architecture is useful.

Keep:

C = H_blend - H_target

with:

owner seam:
    exact owner height

outer edge:
    C = 0

interior:
    harmonic

Retain ordinary first-inland owner-slope anchors on straight seam samples.

Do not attempt to copy multiple owner rows.

Do not propagate raw owner profiles inward.

Do not try to match curvature perfectly yet.

The purpose of this pass becomes:

eliminate giant discontinuities so erosion starts from a coherent surface.

That's all.

11. Move geomorphic generation ahead of final seam perfection

This is now the right place to return to erosion.

The report is especially convincing here: exact height and almost exact first derivative still leave visually obvious mountain borders. That means further local seam fitting is attacking the wrong order of derivative.

The missing ingredient is landform organization.

Run erosion on:

relief-amplified field
+ synthesized missing cells
+ broad harmonic seam bridge

Then do the final exact seam repair afterward.

12. Erosion domain

For each solve region, construct:

generated region
+
owner halo

Use approximately:

4–8 cells owner halo

for the first implementation.

Owner halo heights participate in:

gradients;
routing;
accumulation;
river entry.

But:

owner halo vertices are NEVER modified

Similarly:

exact owner seam vertices remain fixed during erosion

All generated Tamriel and synthesized-hole terrain is editable.

This is simpler and more physically meaningful than injecting arbitrary +2000 flow accumulation at seam vertices.

13. Use an 8-neighbor multi-flow router for the first erosion implementation

Do not return to D8 + jitter.

For every generated/owner-halo vertex, identify lower neighbors among the 8 surrounding samples.

For neighbor j:

s
j
	​

=
d
j
	​

H
i
	​

−H
j
	​

	​


where:

d = 128 GU cardinal
d = 128*sqrt(2) diagonal

Then:

w
j
	​

=
∑
k
	​

max(s
k
	​

,0)
p
max(s
j
	​

,0)
p
	​


Start with:

p = 1.1–1.5

This distributes flow among plausible downhill directions and avoids the hard cardinal/diagonal channel orientation that ruined the old D8 result.

A formal D∞ implementation can replace this later if needed, but MFD is sufficient to get the geomorphic phase moving without another large algorithm project.

14. Resolve sinks before accumulation

Use Priority-Flood or an equivalent deterministic depression-resolution pass on a routing copy of the heightmap.

Important:

routing surface may be depression-adjusted
actual rendered terrain need not be wholesale filled

Use the adjusted field to establish drainage connectivity.

Do not use random epsilon jitter.

15. Carry owner drainage naturally

Because owner terrain is included in the routing halo, owner flow should naturally enter generated terrain wherever downhill geometry crosses the boundary.

If the halo is insufficient for major rivers later, extend accumulation preprocessing farther into owner territory.

Do not restore:

+2000 for every border vertex

or any uniform seam-flow injection.

16. Normalize the stream-power law

Use:

A
^
=A/A
ref
	​


and:

E=KΔt
A
^
m
S
n

Starting values:

{
  "m": 0.5,
  "n": 1.0
}

Do not use raw A^0.8.

Do not use a global 12-GU incision cap as the erosion law.

For explicit stability, if limiting is needed, limit by local available relief:

max_incise = (
    stability_fraction
    * max(
        h_here - h_lowest_receiver,
        0.0,
    )
)

with perhaps:

stability_fraction = 0.2–0.3

That prevents height inversion without making every large river erode at the same arbitrary GU/step rate.

17. Start with a modest number of geomorphic cycles

Because AMG has made the seam system cheap, erosion should also stay targeted.

Start around:

16–24 cycles

Each cycle:

resolve routing surface
compute MFD accumulation
incise generated terrain
perform mild hillslope relaxation
restore fixed owner/seam vertices

Do not do 250 cycles.

Render snapshots after perhaps:

0
4
8
16
24

for the first test only.

18. Hillslope refinement

After incision, apply modest slope relaxation to generated terrain.

Use a stable diffusion/thermal step.

Do not smooth:

owner terrain
exact seam
underwater terrain unnecessarily

The purpose is to prevent razor-thin trenches and create broader valley shoulders.

19. Tiny perturbation is allowed only as a routing symmetry breaker

If a synthesized missing cell is extremely smooth, drainage may be underdetermined.

Allow a very small correlated perturbation:

a few GU to perhaps 10–20 GU maximum

inside generated terrain before routing.

Its purpose is only to break exact symmetry.

It must not be visible as a noise texture.

Do not band-match TR noise.

20. After erosion, perform the real final seam lock

This is where exact border finishing belongs.

Take:

H_eroded

and define a narrow final correction:

C_final = H_final - H_eroded

Use only approximately:

1–2 cells on the generated side

for this final correction.

Boundary conditions:

at owner seam:
    H_final = exact owner height

outer edge of final band:
    C_final = 0

For ordinary non-corner seam vertices, keep the first-inland owner derivative anchor if it is valid.

At corners:

NO special diagonal hard anchor

Let the narrow harmonic field reconcile the two edge directions.

Solve with the same AMG direct Laplace system.

This provides:

exact C0
mostly C1-consistent transition
minimum disturbance to the eroded terrain

without trying to manufacture mountain structure through the seam interpolator itself.

21. Final pixel-perfect requirement belongs after VHGT encoding

The current numerical C0 = 0 shows that the working float field can already match the owner edge exactly.

The final hard requirement should therefore be:

write generated VHGT
reread generated VHGT
decode heights
compare every shared 65-vertex edge to owner

Fail if:

max decoded seam error != 0

This is the actual pixel-perfect seam requirement.

Do not confuse a visually detectable curvature line with an edge-height mismatch.

22. What to stop working on

Do not spend another pass on:

raw owner-profile mirroring;
nearest-seam pixel projection;
edge profile extrusion;
special diagonal corner extrapolation;
making the harmonic pre-erosion surface mimic TR's detailed curvature;
global nearest-edge NaN fill.

All of those either demonstrably failed or solve the wrong stage of the problem. The report documents the three profile-continuation failures directly.

23. Immediate implementation sequence

The agent should do this in one focused milestone:

Add explicit cell_height_source / synthesis-required cell semantics to the corpus.
Stop global fill_missing_from_edges.
AMG-inpaint only required missing-height components from surrounding authoritative terrain.
Remove the special diagonal corner anchor.
Run current broad harmonic seam solve.
Run the first MFD erosion implementation across generated terrain plus fixed owner halo.
Run a 1–2-cell post-erosion harmonic seam lock.
Render the exact same 61-cluster TR review region.
Verify decoded seam equality once authoring exists.
Repeat this across 10 different seam regions from TR, Skyrim and Cyrodil to ensure that this is replicable in nearly any region.
Return visual results before tuning erosion heavily.

The important conceptual change is:

Do not make interpolation responsible for creating TR-like mountains.

Interpolation should create a coherent base and guarantee the final shared edge. Erosion and geomorphic processing should create the terrain between those two jobs.

That is now much more consistent with what the actual failures are telling us.
